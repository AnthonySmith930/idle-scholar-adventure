import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  rowContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'flex-end',
    gap: 4,
    paddingLeft: 4,
    marginBottom: 4
  },
  miniSlotOverride: {
    width: 28,
    height: 28,
    borderWidth: 2
  }
})